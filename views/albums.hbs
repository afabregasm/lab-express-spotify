<h2>{{album.0.artists.0.name}} Albums:</h2>
<div>
    {{#each album}}
    <div>
        <h3>{{name}}</h3>
        <p><img src="{{images.0.url}}" alt="{{name}}" width="500px" height="auto" /></p>
        <p><a href="/tracks/{{id}}">View Tracks</a></p>
    </div>
    {{/each}}
</div>