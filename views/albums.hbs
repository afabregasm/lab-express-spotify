<h2>{{artistId.artist.0.name}} albums:</h2>
<div>
    {{#each artistId}}
    <div>
        <h3>{{name}}</h3>
        <p><img src="{{images.0.url}}" alt="{{name}}" width="20%" height="auto" /></p>
        {{!-- <p><a href="/albums/{{id}}">View Albums</a></p> --}}
    </div>
    {{/each}}
</div>